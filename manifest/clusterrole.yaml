apiVersion: v1
kind: ServiceAccount
metadata:
  name: katib-experiment-reader
  namespace: kubeflow
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: experiment-reader
rules:
  - apiGroups: [ "kubeflow.org" ]
    resources: [ "experiments" ]
    verbs: [ "get", "list", "watch", "update" ]
  - apiGroups: [ "" ]
    resources: [ "configmaps", "pods" ]
    verbs: [ "get", "list" ]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: read-experiment
subjects:
  - kind: ServiceAccount
    name: katib-experiment-reader
    namespace: kubeflow
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: experiment-reader
