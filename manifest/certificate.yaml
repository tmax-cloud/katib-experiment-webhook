apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: katib-experiment-webhook-cert
  namespace: kubeflow
  labels:
    some-annotation: "true"
spec:
  secretName: katib-experiement-webhook-tls
  duration: 2880h
  renewBefore: 360h
  dnsNames:
    - katib-experiment-webhook-svc.kubeflow.svc
    - katib-experiment-webhook-svc.kubeflow.svc.cluster.local    
    - katib-experiment-webhook-svc    
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: ck-selfsigned-clusterissuer
    kind: ClusterIssuer
    group: cert-manager.io